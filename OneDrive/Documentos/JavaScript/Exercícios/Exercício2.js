function Index() {
    this.string = string("Brave New World");
}
const string = "Brave New World";

for (const string in obj) {
    if (obj.hasOwnProperty(prop)) {
        console.log("obj." + prop + " = " + obj[prop]);
    }
}